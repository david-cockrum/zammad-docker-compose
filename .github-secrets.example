# Example GitHub Secrets Configuration
# Copy this to .github-secrets and fill in your actual values
# DO NOT commit the actual .github-secrets file to git!

# Docker Hub Credentials
DOCKER_USERNAME=your-dockerhub-username
DOCKER_PASSWORD=your-dockerhub-access-token

# Optional: Additional secrets for production
POSTGRES_PASS=strong-postgres-password
REDIS_PASSWORD=strong-redis-password
ZAMMAD_SECRET_KEY_BASE=generate-with-openssl-rand-hex-64
